import streamlit as st
import time

# ==========================================
# ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ЗАПУСКУ
# ==========================================
# 1. Убедитесь, что у вас установлен Python (версия 3.8 или выше).
# 2. Установите библиотеку Streamlit, выполнив команду в терминале:
#    pip install streamlit
# 3. Запустите приложение командой:
#    streamlit run lms_app.py
# ==========================================

# Конфигурация страницы
st.set_page_config(
    page_title="LMS Прототип",
    page_icon="📚",
    layout="wide",
    initial_sidebar_state="expanded"
)

# === Session State: Инициализация состояния ===
if 'user_name' not in st.session_state:
    st.session_state['user_name'] = "Гость"
if 'current_level' not in st.session_state:
    st.session_state['current_level'] = 0  # Уровень доступа к модулям (0 - только профиль, 1 - Модуль 1 и т.д.)
if 'score' not in st.session_state:
    st.session_state['score'] = 0
if 'completed_modules' not in st.session_state:
    st.session_state['completed_modules'] = set()
if 'module_1_step' not in st.session_state:
    st.session_state['module_1_step'] = 0
if 'quiz_1_score' not in st.session_state:
    st.session_state['quiz_1_score'] = 0
if 'module_2_step' not in st.session_state:
    st.session_state['module_2_step'] = 0
if 'quiz_2_score' not in st.session_state:
    st.session_state['quiz_2_score'] = 0
if 'module_3_step' not in st.session_state:
    st.session_state['module_3_step'] = 0
if 'quiz_3_score' not in st.session_state:
    st.session_state['quiz_3_score'] = 0
if 'module_4_step' not in st.session_state:
    st.session_state['module_4_step'] = 0
if 'quiz_4_score' not in st.session_state:
    st.session_state['quiz_4_score'] = 0

# === Helper Functions ===
def navigate_to(page, level=None):
    if level is not None:
        st.session_state['current_level'] = max(st.session_state['current_level'], level)
    st.session_state['page_selection'] = page

# === Сайдбар (Навигация) ===
st.sidebar.title("LMS Навигация")

# Определение доступных страниц на основе прогресса
pages = {
    "Профиль": "profile",
    "Модуль 1: Основы": "module_1",
    "Модуль 2: Железо": "module_2",
    "Модуль 3: Сети": "module_3",
    "Модуль 4: Веб": "module_4",
    "Финальный экзамен": "final_exam"
}

# Логика доступа к страницам
available_pages = list(pages.keys())
# Если уровень 0, доступен только профиль (пока не введет имя, например)
# Здесь для простоты показываем все, но блокируем контент, или скрываем пункты.
# Реализуем скрытие недоступных модулей для наглядности прогресса.

# Фильтруем страницы, которые пользователь еще не открыл
# Уровень 0 -> Профиль доступен.
# Уровень 1 -> Профиль + Модуль 1.
# И так далее.
page_options = list(pages.keys())[:st.session_state['current_level'] + 1]

# Если пользователь прошел все, показываем всё
if st.session_state['current_level'] >= 5:
    page_options = list(pages.keys())

# Инициализация выбора страницы в session_state, если её нет или она недоступна
if "page_selection" not in st.session_state:
    st.session_state["page_selection"] = page_options[0]

# Проверка, чтобы выбранная страница была в списке доступных (на случай сброса уровня)
if st.session_state["page_selection"] not in page_options:
    st.session_state["page_selection"] = page_options[0]

selection = st.sidebar.radio("Перейти к разделу:", page_options, key="page_selection")

# Отображение прогресса в сайдбаре
st.sidebar.markdown("---")
st.sidebar.write(f"Текущий счет: {st.session_state['score']} XP")
st.sidebar.write(f"Открыто модулей: {st.session_state['current_level']}/5")


# === Главная страница: Прогресс-бар ===
# Отображается на всех страницах сверху для наглядности
progress_value = min(st.session_state['current_level'] / 5, 1.0)
st.progress(progress_value)


# === Функции Страниц ===

def show_profile():
    st.title("👤 Профиль пользователя")
    
    name = st.text_input("Введите ваше имя", value=st.session_state['user_name'])
    if st.button("Сохранить"):
        st.session_state['user_name'] = name
        st.success(f"Имя обновлено: {name}")
        
    st.write("---")
    st.subheader("Ваш прогресс")
    st.write(f"Текущий уровень доступа: {st.session_state['current_level']}")
    st.write(f"Набрано баллов: {st.session_state['score']}")
    
    if st.session_state['current_level'] == 0:
        st.info("👋 Добро пожаловать! Чтобы начать обучение, нажмите кнопку ниже.")
        if st.button("Начать обучение 🚀", on_click=navigate_to, args=("Модуль 1: Основы", 1)):
            pass

def show_module_1():
    st.title("📘 Модуль 1: Введение в профессию")
    
    # --- Слайды контента ---
    def slide_1():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("1. Что такое \"Тикет\" и зачем он нужен?")
        st.markdown("""
        **Суть за 30 секунд (Аналогия):**  
        Представь, что ты пришел в ресторан. Ты же не бежишь на кухню кричать повару: "Пожарь картошку!". Ты зовешь официанта. Он записывает твой заказ в блокнот или планшет.
        Этот заказ — и есть **Тикет**.
        Если заказа нет в системе — повар не начнет готовить. Если официант забыл передать заказ — ты останешься голодным.

        **Tech Deep Dive (Как это в IT):**  
        В IT "официант" — это **Service Desk** (ты). "Повар" — это **Системный администратор** или программист.
        **Тикет (Ticket / Заявка)** — это документ в специальной программе, где записано:
        1. **Кто** обратился (Клиент).
        2. **Что** случилось (Проблема).
        3. **Когда** нужно решить (Срок).

        **Запомни правило:** "Нет тикета — нет работы". Если тебе позвонили и попросили починить, но тикета нет — считай, ты ничего не делал.
        """)

    def slide_2():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("2. Два главных типа проблем: Инцидент vs Запрос")
        st.markdown("""
        **Суть за 30 секунд (Аналогия):**
        * **Инцидент (Incident):** Ты едешь на машине, и у тебя **пробило колесо**. Машина ехала, а теперь стоит. Нужно срочно чинить, чтобы ехать дальше. Это поломка.
        * **Запрос (Service Request):** Ты едешь на машине, всё хорошо, но ты захотел **установить кондиционер**. Машина едет и без него, но с ним комфортнее. Это улучшение.

        **Tech Deep Dive:**
        * **Инцидент:** "У меня сломалось", "Не работает", "Выдает ошибку". *Цель:* Починить как можно быстрее.
        * **Запрос:** "Выдайте доступ", "Установите программу", "Купите мышку". *Цель:* Сделать всё по инструкции (согласовать с начальником, выдать со склада).

        **Почему это важно?**  
        Потому что "Пробитое колесо" (Инцидент) важнее "Установки кондиционера" (Запрос). Сначала чиним то, что горит.
        """)

    def slide_3():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("3. Жизненный цикл тикета (Путь заявки)")
        st.markdown("""
        **Суть за 30 секунд (Аналогия):**  
        Представь заказ на Ozon или Wildberries.
        1. **Создан:** Ты нажал кнопку "Купить".
        2. **В работе:** Кладовщик ищет товар на полке.
        3. **Ожидание:** Товар закончился, ждем поставку (время доставки сдвигается).
        4. **Решено:** Курьер вручил тебе пакет.
        5. **Закрыто:** Ты поставил 5 звезд в приложении.

        **Tech Deep Dive (Статусы тикета):**
        1. **New (Новый):** Заявка упала в систему. Никто её еще не видел.
        2. **In Progress (В работе):** Ты нажал кнопку "Взять в работу". Клиент видит: "Ага, моим вопросом занимаются".
        3. **Pending (Ожидание):** Ты задал вопрос клиенту ("Пришли скриншот ошибки") и ждешь ответа. В этот момент твой таймер (SLA) стоит на паузе.
        4. **Resolved (Решено):** Ты починил и написал: "Проверяйте".
        5. **Closed (Закрыто):** Клиент подтвердил, что всё ок.
        """)

    def slide_4():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("4. SLA — Твой главный дедлайн")
        st.markdown("""
        **Суть за 30 секунд (Аналогия):**  
        Доставка пиццы: "Доставим за 30 минут или пицца бесплатно".
        Эти "30 минут" — и есть **SLA**.
        Если курьер приехал через 40 минут — пиццерия теряет деньги (штраф).

        **Tech Deep Dive:**  
        **SLA (Service Level Agreement)** — это соглашение об уровне сервиса. Это таймер обратного отсчета.
        * На "Починку принтера" у тебя может быть **8 часов**.
        * На "Падение сайта всей компании" — **15 минут**.
        Если таймер дойдет до 00:00 — это называется **SLA Breach** (Нарушение). За это компанию штрафуют, а тебя могут лишить премии.
        """)
    
    def slide_5():
        st.header("БЛОК 2: Видео и Визуализация")
        st.subheader("1. Видео (На русском, просто о сложном)")
        
        col1, col2 = st.columns(2)
        with col1:
            st.markdown("**Из чего состоит SLA**")
            st.video("https://www.youtube.com/watch?v=FYq5aRT3Cig")
        with col2:
            st.markdown("**Help Desk vs Service Desk**")
            st.video("https://www.youtube.com/watch?v=F-bLEnm1mYw")

        st.divider()
        st.subheader("2. Визуализация (Включаем воображение)")
        st.markdown("""
        Представь, что перед тобой экран монитора.
        * Он разделен на три цвета, как светофор.
        * 🔴 **Красная зона:** Тикеты, где время почти вышло (SLA горит). Их нужно хватать первыми!
        * 🟡 **Желтая зона:** Новые тикеты.
        * 🟢 **Зеленая зона:** Тикеты, где мы ждем ответа от пользователя (Pending). Они не "горят".
        """)

    def slide_6():
        st.header("БЛОК 3: Тренажер (Симуляция для новичка)")
        st.markdown("""
        **Сценарий:** Первый день на работе.  
        **Ситуация:**  
        Тебе звонит бухгалтер Марья Ивановна. Она очень расстроена.  
        **Голос Марьи Ивановны:** *"Алло! У меня ничего не печатает! Я нажимаю кнопку, а принтер молчит! Срочно сделайте что-нибудь, мне отчет нести директору через час!"*

        **Твоя задача:**
        1. Определить: это **Инцидент** (поломка) или **Запрос** (хотелка)?
        2. Что ты должен сделать в самую первую очередь **в системе**?
        3. Марья Ивановна кричит. Как ты её успокоишь, используя термин "Тикет"?
        """)
        
        with st.expander("🕵️ Посмотреть эталонный ответ (Как поступает профи)"):
            st.markdown("""
            **1. Тип проблемы:**  
            Это **Инцидент**. Раньше печатало -> сейчас сломалось -> бизнес страдает.

            **2. Первое действие:**  
            Создать **Тикет**. Даже если решишь за 5 минут, сначала заявка! "Нет тикета — нет работы".

            **3. Ответ пользователю:**  
            > "Марья Ивановна, не волнуйтесь. Я прямо сейчас **регистрирую заявку (тикет)** №123 под максимальным приоритетом. Я вижу вашу проблему в системе и уже начинаю диагностику."
            
            *Почему это работает:* Ты перевел эмоции в рабочий процесс.
            """)

    # --- КВИЗ Слайды ---
    def slide_quiz_1():
        st.header("БЛОК 4: Проверка (Закрепляем базу)")
        st.subheader("Вопрос 1 / 3")
        st.markdown("**К тебе приходит коллега и говорит: \"Слушай, поставь мне Фотошоп, мне срочно надо картинку обрезать\". Всё работает, просто нет программы. Что это?**")
        
        ans = st.radio("Ваш ответ:", ["Инцидент (Incident)", "Запрос на обслуживание (Service Request)", "Дружеская просьба, тикет не нужен"], key="q1_radio")
        
        if st.button("Ответить"):
            if ans == "Запрос на обслуживание (Service Request)":
                st.success("Правильно! Ничего не сломалось, это новое ПО.")
                st.session_state['quiz_1_score'] = 1
                time.sleep(1)
                st.session_state['module_1_step'] += 1
                st.rerun()
            else:
                st.error("Ошибка. Попробуйте еще раз.")
                st.info("Подсказка: Ничего не сломалось. Человек хочет *новое*.")

    def slide_quiz_2():
        st.header("БЛОК 4: Проверка (Закрепляем базу)")
        st.subheader("Вопрос 2 / 3")
        st.markdown("**Зачем нужен статус \"Pending\" (Ожидание)?**")
        
        ans = st.radio("Ваш ответ:", ["Чтобы пойти попить кофе", "Чтобы остановить таймер SLA, пока ждем клиента", "Чтобы удалить тикет"], key="q2_radio")
        
        if st.button("Ответить"):
            if "остановить таймер SLA" in ans:
                st.success("Верно! Это кнопка паузы.")
                st.session_state['quiz_1_score'] = 2
                time.sleep(1)
                st.session_state['module_1_step'] += 1
                st.rerun()
            else:
                st.error("Неверно. Подумайте о времени решения.")

    def slide_quiz_3():
        st.header("БЛОК 4: Проверка (Закрепляем базу)")
        st.subheader("Вопрос 3 / 3")
        st.markdown("**Ты починил проблему и написал клиенту \"Всё готово\". Какой статус ты поставишь?**")
        
        ans = st.radio("Ваш ответ:", ["Closed (Закрыто навсегда)", "Resolved (Решено)", "Удалить тикет"], key="q3_radio")
        
        if st.button("Ответить"):
            if "Resolved" in ans:
                st.success("Отлично! Мы даем время на проверку.")
                st.session_state['quiz_1_score'] = 3
                st.session_state['module_1_step'] += 1
                time.sleep(1)
                st.rerun()
            else:
                st.error("Ошибка. Closed ставится автоматически позже.")

    def slide_finish():
        st.header("🎉 Поздравляем!")
        st.success(f"Вы прошли Модуль 1! Ваш результат: {st.session_state.get('quiz_1_score', 0)}/3")
        st.balloons()
        
        if 1 not in st.session_state['completed_modules']:
             st.session_state['score'] += 100
             st.session_state['completed_modules'].add(1)
             if st.session_state['current_level'] < 2:
                 st.session_state['current_level'] = 2
        
        if st.button("Перейти к Модулю 2", on_click=navigate_to, args=("Модуль 2: Железо",)):
            pass


    # Список слайдов
    slides = [slide_1, slide_2, slide_3, slide_4, slide_5, slide_6, slide_quiz_1, slide_quiz_2, slide_quiz_3, slide_finish]
    
    # Контроллер навигации
    current_step = st.session_state.get('module_1_step', 0)
    
    # Отображаем текущий слайд
    if 0 <= current_step < len(slides):
        slides[current_step]()
    
    # Кнопки навигации (только для теории и видео, квизы сами управляют переходом)
    # Слайды 0-5 - это теория/видео/тренажер. 6,7,8 - квиз. 9 - финиш.
    if current_step < 6:
        col_prev, col_next = st.columns([1, 10])
        with col_prev:
            if current_step > 0:
                if st.button("⬅ Назад"):
                    st.session_state['module_1_step'] -= 1
                    st.rerun()
        with col_next:
            if st.button("Далее ➡"):
                st.session_state['module_1_step'] += 1
                st.rerun()

def show_module_2():
    st.title("📗 Модуль 2: Железо и операционные системы")

    # --- Слайды контента ---
    def slide_1():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("1. Как устроен компьютер: \"Ресторанная кухня\"")
        st.markdown("""
        **Суть за 30 секунд (Аналогия):**  
        Представь, что компьютер — это **кухня ресторана**.

        *   🧑‍🍳 **CPU (Процессор): Это Шеф-повар.**  
            Чем он круче (больше ядер/гигагерц), тем быстрее он режет овощи.

        *   🔪 **RAM (ОЗУ, Оперативная память): Это Разделочный стол.**  
            Повар берет продукты из холодильника и кладет на стол, чтобы резать. Если стол маленький (мало памяти), на нем поместится только одна морковка. Повару придется бегать к холодильнику за каждым огурцом — работа встанет.  
            *Важно:* Когда свет на кухне выключают (выключение ПК), со стола всё убирают. Стол пустой.

        *   ❄️ **SSD/HDD (Жесткий диск): Это Холодильник / Склад.**  
            Там продукты лежат годами, даже без света. Но доставать оттуда долго.

        *   🏠 **Motherboard (Материнская плата): Это само Помещение кухни**, которое соединяет повара, стол и холодильник проводами-дорожками.
        """)

    def slide_2():
        st.header("Tech Deep Dive: Железо")
        st.markdown("""
        *   **CPU (Central Processing Unit):** "Мозг". Выполняет инструкции. Главные параметры: Частота (ГГц) и Количество ядер.

        *   **RAM (Random Access Memory):** "Быстрая память". Энергозависимая (Volatile) — данные стираются при выключении. Нужна для запущенных программ.

        *   **SSD (Solid State Drive) vs HDD (Hard Disk Drive):**
            *   **HDD:** Старый, жужжит, внутри крутится магнитный диск. Дешевый, но медленный.
            *   **SSD:** Новый, как большая флешка. Молниеносный. Для Windows 10/11 наличие SSD обязательно, иначе система будет "тормозить" вечно.
        """)
        st.info("""
        **Real World Context (Зачем это L1?):** Клиент говорит: "Компьютер тормозит". Ты смотришь Диспетчер задач.
        *   Если забит **CPU (100%)**: Зависла программа или вирус.
        *   Если забита **RAM (90%+)**: У пользователя открыто 100 вкладок в Chrome, а "стол" маленький (всего 4 ГБ). Решение: закрыть вкладки или купить память.
        *   Если забит **Disk (100%)**: Скорее всего, стоит старый HDD, который умирает.
        """)

    def slide_3():
        st.header("БЛОК 1: Теория")
        st.subheader("2. BIOS и Загрузка: \"Утренний ритуал\"")
        st.markdown("""
        **Суть за 30 секунд:**  
        Когда повар (CPU) приходит на работу, он еще сонный. Ему нужна инструкция, как включить свет и газ. **BIOS/UEFI** — это эта первая инструкция, "вшитая" в саму кухню. Она проверяет: "Ножи на месте? Газ есть? Свет горит?". Если всё ок, она будит Операционную систему (Директора ресторана).

        **Tech Deep Dive:**
        *   **POST (Power-On Self-Test):** Самопроверка при включении. Если "пищит" и не включается — проверка не прошла (например, сгорела память).
        *   **Boot Order (Порядок загрузки):** Список, где искать Windows. Обычно: 1. USB-флешка -> 2. Жесткий диск.
        """)

    def slide_4():
        st.header("БЛОК 1: Теория")
        st.subheader("3. Драйверы: \"Переводчики\"")
        st.markdown("""
        **Суть за 30 секунд:**  
        Ты купил на кухню японскую рисоварку (Новый принтер). Шеф-повар (Windows) не знает японского. Ему нужна инструкция на русском. **Драйвер** — это и есть эта инструкция-переводчик. Без неё Windows видит "Неизвестное устройство" и не знает, как с ним работать.
        """)

    def slide_5():
        st.header("БЛОК 1: Теория")
        st.subheader("4. Файловая система: Windows vs Linux")
        st.markdown("""
        **Суть за 30 секунд:**

        *   🗄️ **Windows (Офисный архив):**  
            У нас есть разные железные шкафы: Шкаф C, Шкаф D, Шкаф E (Диски). В каждом свои папки.  
            **Адрес:** `C:\\Users\\Ivan\\Desktop` (Обратный слеш `\\`).

        *   🌳 **Linux (Дерево):**  
            У нас нет шкафов. У нас есть Одно гигантское дерево. Всё растет от одного Корня (`/`). Даже флешка — это просто веточка на этом дереве.  
            **Адрес:** `/home/ivan/desktop` (Прямой слеш `/`).
        """)

    def slide_6():
        st.header("БЛОК 2: Видео и Визуализация")
        st.subheader("1. Видео (Смотрим глазами)")
        
        tab1, tab2, tab3 = st.tabs(["Как работает ПК", "BIOS за 5 мин", "Файловая система"])
        
        with tab1:
            st.markdown("**Как работает компьютер (Процессор, ОЗУ, СПЗУ)**")
            st.video("https://www.youtube.com/watch?v=hscJXcQXOS4")
            st.caption("Фокус: Обрати внимание на связь Процессор <-> Память.")
        
        with tab2:
            st.markdown("**BIOS и UEFI за 5 минут**")
            st.video("https://www.youtube.com/watch?v=YHSgf-otxu8")
        
        with tab3:
            st.markdown("**Файловая система Windows**")
            st.video("https://www.youtube.com/watch?v=Rw8U-V7uaLo")

        st.divider()
        st.subheader("2. Visual Guide (Внутренности ПК)")
        st.markdown("""
        Представь (или погугли картинку "Motherboard components diagram"):
        *   🌀 **Большой вентилятор в центре** — под ним Процессор (CPU) (чтобы повар не вспотел).
        *   📏 **Длинные узкие планки рядом** — это ОЗУ (RAM) (тот самый стол).
        *   📦 **Коробочка, подключенная проводом в стороне** — это Диск (SSD).
        """)

    def slide_7():
        st.header("БЛОК 3: Лабораторная работа (Тренажер)")
        st.subheader("Задание 1: Диагностика \"Тормозов\"")
        st.markdown("""
        **Легенда:** Пользователь жалуется, что компьютер медленно работает. Тебе нужно понять, какого ресурса не хватает.

        **Твои действия:**
        1.  Нажми на клавиатуре `Ctrl + Shift + Esc`. Откроется Диспетчер задач (Task Manager).
        2.  Если окно маленькое, нажми "Подробнее" (More details) внизу.
        3.  Перейди на вкладку **Производительность** (Performance).
        4.  Найди графики: CPU, Память (Memory), Диск (Disk).
        """)
        
        st.markdown("### Вопросы для самопроверки:")
        q_ram = st.text_input("Сколько у тебя памяти (RAM)? (Например, 8 ГБ)")
        if q_ram:
            st.caption("Анализ: Если 4 ГБ — это минимум для офиса. Если 16 ГБ — это хорошо.")
        
        q_disk = st.radio("Какой у тебя диск?", ["Не знаю/Не выбрано", "HDD", "SSD"])
        if q_disk == "HDD":
            st.warning("Анализ: Если HDD — вот причина тормозов при загрузке.")
        elif q_disk == "SSD":
            st.success("Анализ: Отлично, система должна летать.")

        st.markdown("""
        *   Запусти браузер с 5 вкладками YouTube. Посмотри, как подскочил график "Память".
        *   **Вывод:** Ты видишь, как "Стол" заполняется продуктами.
        """)

    def slide_8():
        st.header("БЛОК 3: Лабораторная работа (Тренажер)")
        st.subheader("Задание 2: Проверка Драйверов")
        st.markdown("""
        **Легенда:** Пользователь подключил веб-камеру, а она не работает. Нужно проверить драйвер.

        **Твои действия:**
        1.  Нажми правой кнопкой мыши на кнопку "Пуск".
        2.  Выберите **Диспетчер устройств** (Device Manager).
        3.  Посмотри на список. Есть ли там желтые треугольники с восклицательным знаком ⚠️?

        > **ДА:** Значит, драйвер не установлен ("Повар не понимает японскую рисоварку"). Нужно качать драйвер с сайта производителя.  
        > **НЕТ:** Все драйверы на месте. Проблема в другом (например, камера выключена кнопкой).
        """)

    # --- КВИЗ Слайды ---
    def slide_quiz_1():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 1 / 3")
        st.markdown("**Ты выключил компьютер из розетки. Что произойдет с данными?**")
        
        ans = st.radio("Ваш ответ:", [
            "Всё пропадет и с жесткого диска, и из оперативной памяти.",
            "Данные на жестком диске (SSD) останутся, а оперативная память (RAM) очистится.",
            "Данные останутся везде, потому что есть батарейка BIOS."
        ], key="q2_1")
        
        if st.button("Ответить"):
            if "RAM) очистится" in ans:
                st.success("Правильно! RAM — это разделочный стол. Ушел домой — стол убрал.")
                st.session_state['quiz_2_score'] = 1
                time.sleep(1)
                st.session_state['module_2_step'] += 1
                st.rerun()
            else:
                st.error("Ошибка. Вспомните аналогию со столом и холодильником.")

    def slide_quiz_2():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 2 / 3")
        st.markdown("**Пользователь жалуется: \"Компьютер жутко гудит и выключается сам через 15 минут игры/работы\". Что это?**")
        
        ans = st.radio("Ваш ответ:", [
            "Закончилось место на диске C.",
            "Вирус удалил Windows.",
            "Перегрев процессора (CPU Throttling)."
        ], key="q2_2")
        
        if st.button("Ответить"):
            if "Перегрев" in ans:
                st.success("Верно! \"Гудит\" = вентиляторы на максимум. Выключается = защита от перегрева.")
                st.session_state['quiz_2_score'] = 2
                time.sleep(1)
                st.session_state['module_2_step'] += 1
                st.rerun()
            else:
                st.error("Ошибка. Если место кончится, ПК будет тормозить, но не выключится.")

    def slide_quiz_3():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 3 / 3")
        st.markdown("**Тебе нужно написать путь к файлу в Windows. Какой вариант правильный?**")
        
        ans = st.radio("Ваш ответ:", [
            "C:/Users/Admin/File.txt",
            "C:\\Users\\Admin\\File.txt",
            "\\home\\admin\\file.txt"
        ], key="q2_3")
        
        if st.button("Ответить"):
            if "C:\\Users" in ans:
                st.success("Отлично! Windows использует обратный слеш (\\).")
                st.session_state['quiz_2_score'] = 3
                st.session_state['module_2_step'] += 1
                time.sleep(1)
                st.rerun()
            else:
                st.error("Ошибка. Windows любит обратные слеши.")

    def slide_finish():
        st.header("🎉 Финальное задание модуля")
        st.markdown("""
        **Найди в интернете модель своей материнской платы.**
        1.  Нажми `Win + R` (Выполнить).
        2.  Введи `msinfo32` и нажми Enter.
        3.  Найди строчку "Модель основной платы" (BaseBoard Product).
        
        *Зачем:* Саппорт часто просит это сделать пользователя, чтобы найти драйверы именно для его ПК.
        """)
        
        st.success(f"Вы прошли Модуль 2! Ваш результат: {st.session_state.get('quiz_2_score', 0)}/3")
        st.balloons()
        
        if 2 not in st.session_state['completed_modules']:
             st.session_state['score'] += 200
             st.session_state['completed_modules'].add(2)
             if st.session_state['current_level'] < 3:
                 st.session_state['current_level'] = 3
        
        if st.button("Перейти к Модулю 3", on_click=navigate_to, args=("Модуль 3: Сети",)):
            pass

    # Список слайдов
    slides = [slide_1, slide_2, slide_3, slide_4, slide_5, slide_6, slide_7, slide_8, slide_quiz_1, slide_quiz_2, slide_quiz_3, slide_finish]
    
    current_step = st.session_state.get('module_2_step', 0)
    
    if 0 <= current_step < len(slides):
        slides[current_step]()
    
    # Навигация (теория + лабы = слайды 0-7)
    if current_step < 8:
        col_prev, col_next = st.columns([1, 10])
        with col_prev:
            if current_step > 0:
                if st.button("⬅ Назад", key="m2_prev"):
                    st.session_state['module_2_step'] -= 1
                    st.rerun()
        with col_next:
            if st.button("Далее ➡", key="m2_next"):
                st.session_state['module_2_step'] += 1
                st.rerun()

def show_module_3():
    st.title("🌐 Модуль 3: Сети и Интернет. Как данные путешествуют по миру?")
    
    # --- Слайды Module 3 ---
    def slide_1():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("1. IP-адрес и MAC-адрес: \"Где ты живешь?\"")
        st.markdown("""
        **Суть за 30 секунд (Аналогия):**  
        У каждого дома есть адрес (Улица Ленина, д. 5) — это **IP-адрес**. Почтальон (Роутер) знает, куда нести письмо. Но если ты переедешь, адрес изменится.  
        А вот твой отпечаток пальца (или VIN-номер автомобиля) не меняется никогда — это **MAC-адрес**.

        **Tech Deep Dive:**
        *   **IP (Internet Protocol):** Логический адрес. Бывает IPv4 (старый, `192.168.1.1`) и IPv6 (новый, длинный).
        *   **MAC (Media Access Control):** Физический адрес сетевой карты. Вшит на заводе. Выглядит так: `00:1A:2B:3C:4D:5E`.

        **Публичный vs Локальный IP:**
        *   **Публичный:** Твой адрес в Интернете (как адрес дома).
        *   **Локальный (Private):** Твой адрес внутри квартиры (Комната 1, Комната 2). Обычно начинаются на `192.168.x.x` или `10.x.x.x`.

        **Real World Context:**  
        Если пользователь говорит "Я не вижу принтер", скорее всего, у принтера сменился IP-адрес (если он не закреплен), но его MAC остался прежним.
        """)
    
    def slide_2():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("2. DHCP: \"Раздача номеров в отеле\"")
        st.markdown("""
        **Суть за 30 секунд:**  
        Ты приходишь в отель (подключаешься к Wi-Fi). Ты же не выбираешь номер сам ("Хочу номер 101!"). Администратор на ресепшене (**DHCP-сервер**) смотрит в книгу и говорит: "Свободен номер 105. Держи ключи, живи 24 часа".

        **Tech Deep Dive:**  
        **DHCP (Dynamic Host Configuration Protocol)** — автоматически раздает IP-адреса устройствам. Без него тебе пришлось бы вписывать IP вручную на каждом телефоне и ноутбуке.
        """)

    def slide_3():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("3. DNS: \"Телефонная книга интернета\"")
        st.markdown("""
        **Суть за 30 секунд:**  
        Людям удобно запоминать слова (`google.com`), а компьютерам — цифры (`142.250.185.78`).  
        **DNS (Domain Name System)** — это сервис, который переводит слова в цифры.  
        Ты пишешь: "Позвони маме". Телефон находит номер "8-900...".  
        Ты пишешь: `vk.com`. DNS находит IP `87.240.137.158`.

        **Common Pitfall (Ловушка):**  
        Если интернет работает (Skype/Telegram звонят — они работают по IP), а сайты не открываются — это на 99% проблема с DNS.
        """)
        
    def slide_4():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("4. Порты: \"Квартиры в доме\"")
        st.markdown("""
        **Суть за 30 секунд:**  
        IP-адрес — это адрес дома. Но внутри дома много квартир.
        *   **Веб-сайты** живут в квартире №80 (или 443).
        *   **Почта** живет в квартире №25.
        *   **Удаленный доступ** — в квартире №3389.  
        Когда пакет приходит на IP, он стучится в конкретную "дверь" (Порт).
        """)

    def slide_5():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("5. Модель OSI (Очень упрощенно)")
        st.markdown("""
        **Суть за 30 секунд:**  
        Это 7 слоев упаковки посылки.

        *   **L1 (Физический):** Кабель, по которому бежит ток. Сломалось тут? Проверь, воткнут ли провод.
        *   **L2 (Канальный):** Коммутатор (Switch) и MAC-адреса.
        *   **L3 (Сетевой):** Роутер и IP-адреса. Маршрут посылки.
        *   **L4 (Транспортный):** TCP/UDP. Гарантия доставки (доехало ли целым?).
        *   **L7 (Прикладной):** То, что видит пользователь (Браузер, Картинка).
        """)
        
    def slide_6():
        st.header("БЛОК 2: Видео и Визуализация")
        st.subheader("1. Видео (Русский YouTube)")
        st.markdown("Смотрим обязательно. Без этого сети останутся магией.")

        tab1, tab2, tab3 = st.tabs(["Как раб. Интернет", "DNS за 5 мин", "Модель OSI"])
        with tab1:
            st.video("https://www.youtube.com/watch?v=xnx2JDSV87Y")
            st.caption("База. Как твой запрос летит через океан.")
        with tab2:
            st.video("https://www.youtube.com/watch?v=t2NMbSarXC4")
            st.caption("Почему без DNS ты не откроешь VK.")
        with tab3:
            st.video("https://www.youtube.com/watch?v=je0QFU7p5Oo")
            st.caption("Зачем нужны эти 7 уровней.")

        st.divider()
        st.subheader("2. Visual Guide (Команды диагностики)")
        st.markdown("""
        Представь черное окно командной строки (`cmd`).

        *   **Ping (`ping 8.8.8.8`):** Ты кидаешь мячик в стену (Google) и ждешь, отскочит ли он обратно. Если отскочил — стена (сервер) на месте.  
            *Тонкость:* Иногда сервер работает (сайт открывается), а Ping не проходит (админ запретил мячики).

        *   **Tracert (`tracert google.com`):** Ты кидаешь мячик, который оставляет след на каждом столбе по пути. Ты видишь весь маршрут: Твой Роутер -> Провайдер -> Магистраль -> Google. Где оборвалось — там и проблема.
        """)

    def slide_7():
        st.header("БЛОК 3: Лабораторная работа (Симулятор Саппорта)")
        st.subheader("Сценарий: \"Сломался корпоративный портал\"")
        st.markdown("""
        Сейчас ты почувствуешь себя настоящим сетевым инженером. Мы будем искать, где сломался интернет.

        **Легенда:** Пользователь звонит в панике: "Я не могу зайти на корпоративный портал `work.company.local`! Пишет 'Страница недоступна'. Но YouTube работает и музыка играет!"

        **Твои действия (Симуляция):**
        """)
        
        with st.expander("Шаг 1: Проверка связи (Ping)"):
            st.code("ping 8.8.8.8", language="bash")
            st.text("""Ответ от 8.8.8.8: число байт=32 время=20мс TTL=116
Ответ от 8.8.8.8: число байт=32 время=21мс TTL=116""")
            st.success("Анализ: Интернет (кабель, роутер, провайдер) работает! Пакеты уходят в мир и возвращаются.")
            
        with st.expander("Шаг 2: Проверка имен (DNS)"):
            st.code("ping work.company.local", language="bash")
            st.error("При проверке связи не удалось обнаружить узел work.company.local. Проверьте имя узла.")
            st.warning("Анализ: Компьютер не понимает, какой IP-адрес у этого имени. Проблема в DNS!")
            
        with st.expander("Шаг 3: Глубокая проверка (NSlookup)"):
            st.code("nslookup work.company.local", language="bash")
            st.text("""Server:  UnKnown
Address:  192.168.1.1
*** UnKnown can't find work.company.local: Non-existent domain""")
            st.info("Вердикт: Компьютер спрашивает у домашнего роутера (192.168.1.1). Решение: Включить VPN!")

    # КВИЗЫ
    def slide_quiz_1():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 1 / 3")
        st.markdown("**Команда `ping google.com` выдает ошибку \"Не удалось обнаружить узел\", но при этом `ping 8.8.8.8` работает отлично. В чем проблема?**")
        
        ans = st.radio("Ваш ответ:", [
            "У пользователя отключен Интернет.",
            "Сгорела сетевая карта (MAC-адрес).",
            "Проблема с DNS-сервером (не преобразует имена).",
            "Сайт Google закрылся навсегда."
        ], key="q3_1")
        
        if st.button("Ответить"):
            if "DNS" in ans:
                st.success("Правильно! По цифрам пускает, по имени — нет = DNS.")
                st.session_state['quiz_3_score'] = 1
                time.sleep(1)
                st.session_state['module_3_step'] += 1
                st.rerun()
            else:
                st.error("Ошибка. Вспомните про телефонную книгу.")

    def slide_quiz_2():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 2 / 3")
        st.markdown("**На каком уровне модели OSI работает кабель (витая пара), который ты втыкаешь в компьютер?**")
        
        ans = st.radio("Ваш ответ:", [
            "L7 (Прикладной) — потому что я его прикладываю.",
            "L3 (Сетевой) — потому что это сеть.",
            "L1 (Физический) — потому что это физический провод."
        ], key="q3_2")
        
        if st.button("Ответить"):
            if "L1" in ans:
                st.success("Верно! Физика, ток, кабель = Level 1.")
                st.session_state['quiz_3_score'] = 2
                time.sleep(1)
                st.session_state['module_3_step'] += 1
                st.rerun()
            else:
                st.error("Ошибка. Это самый низкий уровень.")

    def slide_quiz_3():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 3 / 3")
        st.markdown("**Зачем нужен DHCP?**")
        
        ans = st.radio("Ваш ответ:", [
            "Чтобы защищать компьютер от вирусов.",
            "Чтобы автоматически раздавать IP-адреса устройствам в сети.",
            "Чтобы ускорять интернет."
        ], key="q3_3")
        
        if st.button("Ответить"):
            if "автоматически раздавать IP" in ans:
                st.success("Отлично! Администратор отеля раздает ключи.")
                st.session_state['quiz_3_score'] = 3
                st.session_state['module_3_step'] += 1
                time.sleep(1)
                st.rerun()
            else:
                st.error("Неверно.")
    
    def slide_finish():
        st.header("🎉 Финальное задание модуля")
        st.markdown("""
        1. Открой командную строку (`Win + R` -> `cmd`).
        2. Напиши `ipconfig /all`.
        3. Найди строку "Физический адрес" (Physical Address). Это твой **MAC**.
        4. Найди строку "IPv4-адрес". Это твой **локальный IP**.
        5. Теперь зайди в браузер и напиши "мой ip". Сайт покажет тебе **Публичный IP**. Сравни их. Они разные. Поздравляю, теперь ты видел NAT в действии!
        """)
        
        st.success(f"Вы прошли Модуль 3! Ваш результат: {st.session_state.get('quiz_3_score', 0)}/3")
        st.balloons()
        
        if 3 not in st.session_state['completed_modules']:
             st.session_state['score'] += 300
             st.session_state['completed_modules'].add(3)
             if st.session_state['current_level'] < 4:
                 st.session_state['current_level'] = 4
        
        if st.button("Перейти к Модулю 4", on_click=navigate_to, args=("Модуль 4: Веб",)):
            pass

    # Список слайдов
    slides = [slide_1, slide_2, slide_3, slide_4, slide_5, slide_6, slide_7, slide_quiz_1, slide_quiz_2, slide_quiz_3, slide_finish]
    current_step = st.session_state.get('module_3_step', 0)
    
    if 0 <= current_step < len(slides):
        slides[current_step]()
        
    if current_step < 7: # Theory + Lab slides
        col_prev, col_next = st.columns([1, 10])
        with col_prev:
            if current_step > 0:
                if st.button("⬅ Назад", key="m3_prev"):
                    st.session_state['module_3_step'] -= 1
                    st.rerun()
        with col_next:
            if st.button("Далее ➡", key="m3_next"):
                st.session_state['module_3_step'] += 1
                st.rerun()

def show_module_4():
    st.title("🌐 Модуль 4: Веб-технологии и диагностика (L1)")
    
    # --- Слайды Module 4 ---
    def slide_1():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("1. HTTP-запрос: \"Заказ в ресторане\"")
        st.markdown("""
        **Суть за 30 секунд (Аналогия):**  
        Представь, что **Браузер (Клиент)** — это посетитель ресторана. **Сервер** — это кухня. Ты не можешь просто зайти на кухню и взять еду. Ты зовешь официанта (**Протокол HTTP**) и делаешь заказ.

        *   **Метод GET:** Ты просишь меню или блюдо. "Принесите мне Цезарь" (Ты просто хочешь получить информацию).
        *   **Метод POST:** Ты отдаешь ингредиенты или заполняешь анкету. "Вот мои данные для карты лояльности" (Ты отправляешь данные на сервер, например, логин/пароль).

        **Tech Deep Dive:**
        *   **HTTP/HTTPS:** Язык, на котором общаются компьютеры. **S (Secure)** в конце значит, что официант носит еду в бронированном чемодане (шифрование), и никто не плюнет в суп по дороге.
        *   **JSON:** Это формат, в котором официант записывает заказ. Это просто текст с фигурными скобками.  
            *Пример:* `{"блюдо": "пицца", "размер": "большая"}`.
        """)
        
    def slide_2():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("2. Коды ответов (Status Codes): \"Что сказал повар?\"")
        st.markdown("""
        **Суть за 30 секунд:**  
        Официант вернулся с кухни. Что он сказал?

        *   🟢 **2xx (Всё ОК):** "Вот ваш заказ". (Код **200 OK**).
        *   🔵 **3xx (Переезд):** "Мы переехали в соседнее здание". (Код **301 Redirect**). Браузер сам пойдет по новому адресу.
        *   🟠 **4xx (Виноват КЛИЕНТ):**
            *   **400 Bad Request:** "Вы сказали какую-то ерунду, повар не понял". (Кривой запрос).
            *   **401 Unauthorized:** "Покажите паспорт/билет". (Ты не залогинился).
            *   **403 Forbidden:** "Вам сюда нельзя, это VIP-зона". (Залогинился, но нет прав).
            *   **404 Not Found:** "Такого блюда нет в меню". (Страница удалена или опечатка в ссылке).
        *   🔴 **5xx (Виноват СЕРВЕР):**
            *   **500 Internal Server Error:** "На кухне пожар, повар в обмороке". (Сломался код на сервере).
            *   **502 Bad Gateway:** "Поставщик продуктов не приехал". (Один сервер не смог достучаться до другого).

        **Real World Context (Главное правило L1):**  
        *   Если ошибка **4xx** — чини браузер пользователя (чисти кэш, проверяй права, правильность ссылки).
        *   Если ошибка **5xx** — звони админам/разработчикам. Ты тут бессилен, сервер "лежит".
        """)

    def slide_3():
        st.header("БЛОК 1: Теория и Погружение")
        st.subheader("3. Cookies и LocalStorage: \"Память официанта\"")
        st.markdown("""
        **Суть за 30 секунд:**

        *   🍪 **Cookies (Куки):** Это карта лояльности, которую ты показываешь официанту каждый раз. Там написано "Я — Иван, столик №5". Если потеряешь куку — сервер забудет, что ты уже заказывал, и попросит снова войти (залогиниться).
        *   📒 **LocalStorage:** Это твой личный блокнот на столе. Сервер туда не смотрит. Там браузер хранит настройки (например, "Темная тема" или "Незаконченный комментарий").
        """)

    def slide_4():
        st.header("БЛОК 2: Видео и Визуализация")
        st.subheader("1. Видео (Русский YouTube)")
        st.markdown("Это нужно видеть глазами.")
        
        tab1, tab2 = st.tabs(["DevTools Intro", "HTTP Codes"])
        with tab1:
            st.video("https://www.youtube.com/watch?v=GyKY5kIRP00")
            st.caption("DevTools для самых маленьких. Как нажать F12 и не испугаться.")
        with tab2:
            st.video("https://www.youtube.com/watch?v=48eCbhTVsac")
            st.caption("Коды состояния HTTP за 5 минут. Разница между 404 и 500.")

    def slide_5():
        st.header("БЛОК 2: Визуализация")
        st.subheader("2. Visual Guide (Консоль разработчика)")
        st.markdown("""
        Представь, что ты открыл капот машины (нажал **F12** в браузере). Тебе нужны только две вкладки:

        1.  **Console (Консоль):** Красный текст здесь — это крики о помощи самой страницы ("Я не могу найти картинку!", "Скрипт сломался!").
        2.  **Network (Сеть):** Это лог всех "официантов".
            *   Видишь красную строку? Нажми на неё.
            *   Смотри **Status** (403? 500?).
            *   Смотри **Response** (Что ответил повар? Часто там написано: "Пароль истек").
        """)

    def slide_6():
        st.header("БЛОК 3: Лабораторная работа (Тренажер Саппорта)")
        st.subheader("Задание 1: \"Охота на ошибку 404\"")
        st.markdown("""
        **Легенда:** Пользователь говорит: "На сайте не грузится логотип компании!"
        
        **Твои действия (прямо сейчас):**
        1.  Нажми **F12** (или ПКМ -> Просмотреть код).
        2.  Перейди на вкладку **Network**.
        3.  Нажми **F5** (обнови страницу).
        4.  В фильтре нажми **Img**.
        5.  Найди любую картинку со статусом **200**. Это значит "ОК".
        
        *Мысленный эксперимент:* Если бы логотип не грузился, строка была бы Красной, а статус — **404**.
        """)
        
        st.divider()
        
        st.subheader("Задание 2: \"Почему кнопка не работает?\"")
        st.markdown("""
        **Легенда:** Клиент звонит: "Я нажимаю кнопку 'Купить', она на секунду сереет, а потом снова синяя. И ничего не происходит! Заказ не создается!"
        
        **Твои действия:**
        1.  Открыть DevTools -> Network.
        2.  Нажать кнопку "Купить".
        3.  Увидеть красную строку `order_create`.
        4.  Кликнуть и посмотреть статус.
        """)
        
        status_case = st.selectbox("Выберите статус ошибки, который вы увидели:", ["Выберите...", "500 Internal Server Error", "403 Forbidden", "400 Bad Request"])
        
        if status_case == "500 Internal Server Error":
            st.error("Диагноз: Сервер сломался. Действие: Эскалация на разработчиков (L3).")
        elif status_case == "403 Forbidden":
            st.warning("Диагноз: Клиенту запрещено. Действие: Проверить права/подписку.")
        elif status_case == "400 Bad Request":
            st.info("Диагноз: Ошибка данных (нет товара). Действие: Объяснить клиенту.")

    def slide_7():
        st.header("БЛОК 3: Лабораторная работа")
        st.subheader("Задание 3: Составление Баг-репорта")
        st.markdown("""
        Ты нашел ошибку 500. Разработчик не будет слушать "Там кнопка не работает". Ему нужны факты.
        
        **Задание:** Напиши баг-репорт по шаблону.
        """)
        
        bug_report = st.text_area("Твой Баг-репорт:", height=150, placeholder="Summary: ...\\nSteps to reproduce: ...\\nActual Result: ...\\nPayload: ...\\nResponse: ...")
        
        if st.button("Проверить репорт"):
            if len(bug_report) > 20:
                st.success("Отлично! Главное — указать Шаги (Steps) и Ответ сервера (Response).")
            else:
                st.warning("Слишком коротко. Посмотри шаблон в задании.")
                
        with st.expander("Показать эталонный баг-репорт"):
            st.code("""Summary: Не работает кнопка "Купить" в корзине (Ошибка 500).
Steps to reproduce:
1. Положить товар в корзину.
2. Открыть DevTools (Network).
3. Нажать "Купить".

Actual Result: Кнопка не реагирует. В Network запрос POST /api/buy возвращает статус 500.
Payload: {"product_id": 123, "count": 1}.
Response: {"error": "Critical database failure"}.""")

    # --- КВИЗЫ ---
    def slide_quiz_1():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 1 / 3")
        st.markdown("**Клиент жалуется на \"Белый экран\" при входе. В консоли ошибка 401 Unauthorized. Что это?**")
        
        ans = st.radio("Ваш ответ:", [
            "Сервер сгорел.",
            "Страница удалена.",
            "Пользователь не авторизован (протухли куки).",
            "У пользователя отключен интернет."
        ], key="q4_1")
        
        if st.button("Ответить"):
            if "не авторизован" in ans:
                st.success("Верно! 401 = Предъявите пропуск.")
                st.session_state['quiz_4_score'] = 1
                time.sleep(1)
                st.session_state['module_4_step'] += 1
                st.rerun()
            else:
                st.error("Ошибка. 401 — это про доступ.")

    def slide_quiz_2():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 2 / 3")
        st.markdown("**В чем разница между GET и POST?**")
        
        ans = st.radio("Ваш ответ:", [
            "GET — получение данных (меню), POST — отправка данных (заказ).",
            "GET — быстрее, POST — медленнее.",
            "GET — безопасный, POST — с вирусами."
        ], key="q4_2")
        
        if st.button("Ответить"):
            if "получение данных" in ans:
                st.success("Правильно!")
                st.session_state['quiz_4_score'] = 2
                time.sleep(1)
                st.session_state['module_4_step'] += 1
                st.rerun()
            else:
                st.error("Неверно.")

    def slide_quiz_3():
        st.header("БЛОК 4: Проверка (Assessment)")
        st.subheader("Вопрос 3 / 3")
        st.markdown("**Вы видите ошибку 503 Service Unavailable. Что посоветовать клиенту?**")
        
        ans = st.radio("Ваш ответ:", [
            "Переустановите Windows.",
            "Очистите куки.",
            "Подождите 5-10 минут, на сервере работы.",
            "Купите новый роутер."
        ], key="q4_3")
        
        if st.button("Ответить"):
            if "Подождите" in ans:
                st.success("Верно! 5xx — это вина сервера. Клиент только ждет.")
                st.session_state['quiz_4_score'] = 3
                st.session_state['module_4_step'] += 1
                time.sleep(1)
                st.rerun()
            else:
                st.error("Ошибка. 503 — сервер недоступен временно.")
    
    def slide_finish():
        st.header("� Финиш Модуля 4")
        st.info("Не бойся красного текста в консоли. Для обычного человека это \"Матрица\", а для тебя — улики.")
        
        st.success(f"Вы прошли Модуль 4! Ваш результат: {st.session_state.get('quiz_4_score', 0)}/3")
        st.balloons()
        
        if 4 not in st.session_state['completed_modules']:
             st.session_state['score'] += 400
             st.session_state['completed_modules'].add(4)
             if st.session_state['current_level'] < 5:
                 st.session_state['current_level'] = 5
        
        if st.button("Перейти к Финальному экзамену", on_click=navigate_to, args=("Финальный экзамен",)):
            pass

    # Список слайдов
    slides = [slide_1, slide_2, slide_3, slide_4, slide_5, slide_6, slide_7, slide_quiz_1, slide_quiz_2, slide_quiz_3, slide_finish]
    current_step = st.session_state.get('module_4_step', 0)
    
    if 0 <= current_step < len(slides):
        slides[current_step]()
        
    if current_step < 7: # Theory + Lab slides
        col_prev, col_next = st.columns([1, 10])
        with col_prev:
            if current_step > 0:
                if st.button("⬅ Назад", key="m4_prev"):
                    st.session_state['module_4_step'] -= 1
                    st.rerun()
        with col_next:
            if st.button("Далее ➡", key="m4_next"):
                st.session_state['module_4_step'] += 1
                st.rerun()

def show_final_exam():
    st.write("Поздравляем! Вы дошли до финала.")
    
    st.write(f"Уважаемый {st.session_state['user_name']}, ответьте на последний вопрос.")
    
    with st.expander("Открыть задание экзамена", expanded=True):
        st.write("Напишите функцию на Python, которая возвращает 'Hello World'.")
        st.code("def hello():\n    return '...'", language='python')
    
    final_ans = st.radio("Что должна вернуть функция?", ["'Hello World'", "print('Hello World')", "Ничего (None)"])
    
    if st.button("Завершить курс"):
        if final_ans == "'Hello World'":
            st.success("🎉 Вы успешно завершили курс!")
            if 3 not in st.session_state['completed_modules']:
                st.session_state['score'] += 50
                st.session_state['completed_modules'].add(3)
                st.balloons()
        else:
            st.error("Неверный ответ. Функция должна возвращать значение.")

# === Маршрутизация ===
if selection == "Профиль":
    show_profile()
elif selection == "Модуль 1: Основы":
    show_module_1()
elif selection == "Модуль 2: Железо":
    show_module_2()
elif selection == "Модуль 3: Сети":
    show_module_3()
elif selection == "Модуль 4: Веб":
    show_module_4()
elif selection == "Финальный экзамен":
    show_final_exam()
